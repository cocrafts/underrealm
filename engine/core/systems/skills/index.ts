import type { ComponentType } from '../../components';
import type { Entity } from '../../ecs';

export const destroyFacingMinHealthSystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

export const gainAttackByEnemyDefenseSystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

export const gainAttackByEnemyMissingHealthSystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

export const gainAttackByRemainingHealthSystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

export const mutateEnemySystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

export const mutateRandomEnemySystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

export const mutatePlayerSystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

// IgnoreEnemyDefense = 'IgnoreEnemyDefense',
// SelfBuff = 'SelfBuff',
// BuffAgainstSameEnemy = 'BuffAgainstSameEnemy',
// BuffNextTurn = 'BuffNextTurn',
// FixedCleaver = 'FixedCleaver',
// FactorCleaver = 'FactorCleaver',
// MultiplyDamageAgainst = 'MultiplyDamageAgainst',
// DoubleAttack = 'DoubleAttack',
// Transform = 'Transform',

export const ignoreEnemyDefenseSystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

export const selfBuffSystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

export const buffAgainstSameEnemy = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

export const buffNextTurnSystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

export const fixedCleaverSystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

export const factorCleaverSystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

export const multiplyDamageAgainstSystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};
export const doubleAttackSystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};

export const transformSystem = () => {
	return {
		// eslint-disable-next-line @typescript-eslint/no-unused-vars
		update: (entities: Entity<ComponentType>[]) => {},
	};
};
