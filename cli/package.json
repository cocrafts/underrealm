{
  "name": "@underrealm/cli",
  "description": "",
  "version": "0.0.1",
  "bin": {
    "ur": "./dist/cli/index.js"
  },
  "main": "index.ts",
  "scripts": {
    "build": "rimraf dist && tsc && node ./fiximports.mjs",
    "setup": "yarn build && npm link",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@underrealm/api": "*",
    "chalk": "4.1.2",
    "csv-parse": "^5.5.6",
    "dotenv": "^16.4.5",
    "prompts": "^2.4.2",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/prompts": "^2",
    "replace-in-file": "^8.2.0",
    "rimraf": "^6.0.1",
    "typescript": "5.4.5"
  }
}
