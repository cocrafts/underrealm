{
  "name": "@underrealm/cli",
  "description": "",
  "version": "0.0.1",
  "bin": {
    "ur": "./dist/index.js"
  },
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "setup": "rimraf dist && yarn build && npm link",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@underrealm/api": "*",
    "csv-parse": "^5.5.6",
    "dotenv": "^16.4.5",
    "prompts": "^2.4.2",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/prompts": "^2",
    "rimraf": "^6.0.1",
    "typescript": "5.4.5"
  }
}
